import { TLocalStorage } from "./constants.types";

// local storage
export const LOCAL_STORAGE_AUTH: TLocalStorage = "storage_auth";
export const LOCAL_STORAGE_PREFERENCES: TLocalStorage = "storage_preferences";

// api
export const API_PATH = import.meta.env.VITE_BACKEND_API_URL;
